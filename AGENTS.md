# walrus-special-2026 · Agent Contract

This project uses `hackathonhunter` in full mode. The user explicitly corrected the workflow once: this must be a new project and must follow the full Hunter process, not a rewrite of an older SuiOverflow entry.

## Workflow

```txt
G0 Prize thesis -> G1 Concept lock -> G2 PRD/UIUX -> G3 Visual contract -> G4 Real loop -> G5 Public smoke -> G6 Package -> G7 Submit
```

## Non-negotiables

- Do not implement product code before G2 PRD and UIUX are complete.
- Full GPT Pro tournament mode is active. Keep cycles separate: R0 research, I1-I3 ideation, local synthesis, J1-J3 judging, local lock, PRD spec, UIUX spec.
- I1-I3 and J1-J3 must be batch-started before awaiting any one run.
- One GPT Pro response cannot satisfy more than one artifact type.
- The selected product must pass Audience Proximity, Product Ambition, Judge Magnet, and Agent-Native bars.
- The project must target `Special - Walrus` and put Walrus on the critical path for agent memory or verifiable data.
- Frontend/UI work later requires `impeccable` plus `design-taste-frontend`, recorded in `.hunter/external-skill-usage.json`.
- Public copy later requires `stop-slop` plus `humanizer` or `humanizer-zh`.
- Pitch deck work requires `ppt-master`; if unavailable, block or ask for a named fallback.
- Polished video work requires `hyperframes`, real app recording, measured TTS durations, and no silent provider fallback.

## Current gate

Active gate: G7/submit hold.

G0-G6 are complete. The public URL smoke passed on desktop, mobile, Chromium, Firefox, and WebKit. Stop before final form submission unless the user explicitly approves that submit action.
